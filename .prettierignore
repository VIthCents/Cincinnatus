dist
node_modules
coverage
.data
.models
.tsbuild
src-tauri/target
src-tauri/gen
pnpm-lock.yaml

# Recorded API responses. These are verbatim bytes from a real endpoint and
# must not be reformatted, or they stop being evidence of what the API returned.
fixtures/raw
